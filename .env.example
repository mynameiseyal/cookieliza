# Admin Panel Configuration
# IMPORTANT: Change this to a strong password in production!
NEXT_PUBLIC_ADMIN_PASSWORD=cookie2010

# Site Configuration (optional - defaults provided in lib/config.ts)
NEXT_PUBLIC_SITE_URL=https://cookieliza.vercel.app
NEXT_PUBLIC_CONTACT_PHONE=050-123-4567
NEXT_PUBLIC_CONTACT_EMAIL=info@cookieliza.co.il

# Instructions:
# 1. Copy this file to .env.local
# 2. Update the values with your actual configuration
# 3. Never commit .env.local to git (it's already in .gitignore)
